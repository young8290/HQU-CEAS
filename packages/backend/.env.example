# ─────────────────────────── 生产部署必读 ───────────────────────────
# 1) JWT_SECRET 生产环境【必须】设置为强随机值：
#      NODE_ENV=production 且未设置 JWT_SECRET 时，后端会拒绝启动（fail-fast）；
#      开发环境缺失仅告警并回退到不安全的开发默认值（切勿用于公网）。
#    生成命令：
#      node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# 2) 公网单源部署（start-prod.bat + cloudflared）时，CORS_ORIGIN 填公网域名，
#    例如 https://zongce.youngspace.top（多个来源用英文逗号分隔）。
# 3) 详细步骤与安全清单见 docs/DEPLOYMENT.md。
# ────────────────────────────────────────────────────────────────────

DATABASE_URL="file:./dev.db"
JWT_SECRET="replace-with-a-long-random-secret"
PORT=4000
CORS_ORIGIN="http://localhost:3000"

# 生产模式（NODE_ENV=production）自动服务 packages/frontend/dist；
# 开发模式如需验证单源静态服务，可取消下行注释（需先构建前端）。
# SERVE_STATIC=1
